---
platform: linux

image_resource:
  type: docker-image
  source: { repository: docker.***REMOVED***/monsoon/arc-pipeline, tag: 'current'}

inputs:
- name: bin
- name: arc.version

run:
  path: sh
  args:
    - -exc
    - |
      /tasks/checksums bin/
params:
  DOMAIN:
  OS_USERNAME:
  OS_PASSWORD:
  CONTAINER:
  OS_PROJECT_ID:
  OS_AUTH_URL:

