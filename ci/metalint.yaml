---
platform: linux

image_resource:
  type: docker-image
  source: { repository: docker.***REMOVED***/monsoon/gobuild, tag: '1.6'}

inputs:
- name: arc.git 

run:
  path: sh
  args:
    - -exc
    - |
      make -C arc.git/ metalint
