---
platform: linux

image_resource:
  type: docker-image
  source: { repository: docker.***REMOVED***/monsoon-concourse/arc-pipeline, tag: 'current'}

inputs:
  - name: source

run:
  path: sh
  args:
    - -exc
    - |
      /tasks/test source/$SUBDIR
params:
  SUBDIR: 
