---
platform: linux

image_resource:
  type: docker-image
  source: { repository: hub.***REMOVED***/monsoon/gobuild, tag: '1.7'}

inputs:
- name: arc.git 

run:
  path: sh
  args:
    - -exc
    - |
      make -C arc.git/ unit
