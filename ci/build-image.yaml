---
platform: linux

image_resource:
  type: docker-image
  source: { repository: docker.***REMOVED***/monsoon-concourse/arc-pipeline, tag: 'current'}

inputs:
  - name: context
  - name: version

output:
  - name: image


run:
  path: sh
  args:
    - -exc
    - |
      DIR=${DIR:-context/}
      /tasks/build $DIR
params:
  DIR: 
