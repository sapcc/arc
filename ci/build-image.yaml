---
platform: linux

image_resource:
  type: docker-image
  source: { repository: docker.***REMOVED***/monsoon-concourse/arc-pipeline, tag: 'current'}

inputs:
  - name: context
  - name: version

outputs:
  - name: image

run:
  path: sh
  args:
    - -exc
    - |
      cd image
      /tasks/build ../context/$SUBDIR ../version
params:
  DIR: 
